# Phase 10 — Adversarial correctness

> Status: `in_progress` · Directive and exit signal: [README](../README.md)

## Tasks

Each row is a unit of work — big enough to be a task, not an entire major project, and not a single trivial change. Progress is a few sentences max; for anything that grows beyond that, log the detail in [`scratchpad.md`](scratchpad.md) and keep the cell short. The last row is always a phase-verification task — re-read the directive and exit signal, walk the docs the phase touches, and confirm nothing was missed. If something was, add rows above the verification row and rerun.

| # | Task | Progress |
|---|---|---|
| 1 | **Surface audit** — Re-read this phase's directive/exit signal and the correctness surfaces (`testing/oracles-lib`, `testing/scenario-runner`, the nemesis profiles, the CI release gate, the updated guarantees in [`docs-next/vision/guarantees.md`](../../docs-next/vision/guarantees.md)). Expand this placeholder into a concrete task list. | `complete` — Audit found that the release gate already covers `testing/scenarios` across both runtimes and all current nemeses, but the new Phase 10 invariants need stronger scenario/oracle coverage and one likely Runner credential-boundary fix. Details are in the scratchpad. |
| 2 | **Credential-less Runner and native-crash blast radius** — Stop Runner children from inheriting Broker/control credentials, add a Runner-crash injection path, emit `runner.crash` with `affectedGameIds`, and strengthen `crash-blast-radius` so it proves both packed `K > 1` and strict `K = 1` behavior. | `complete` — Runner children now get a tight bootstrap env, the Broker can inject and recover from Runner child crashes, `runner.crash` records `affectedGameIds` plus `maxAssignedGames`, and the runner-crash nemesis/scenario proves packed and strict K-bound blast radius locally. |
| 3 | **Checkpoint durability and snapshot consistency** — Add adversarial state/blob churn that crashes around checkpoint boundaries, assert zero loss on planned transitions, assert bounded loss on unplanned transitions, and prove state/blob roots never skew. | `complete` — Added interval checkpoint scheduling, the `checkpoint-skew-probe` bundle, planned `evict-games` workload support, and a checkpoint consistency scenario/oracle that proves interval flushes, unplanned Runner-crash rollback to the last checkpoint, planned-transition zero loss, and no state/blob skew across both runtimes and all nemeses. |
| 4 | **Conditional-PUT fencing** — Build a superseded-shard write race that forces the late checkpoint to hit the conditional root PUT conflict and verifies the stale owner stands down without corrupting the new head. | `to_do` |
| 5 | **Time-travel view and restore-forward** — Wire the admin snapshot/checkpoint/restore surface to the state-store checkpoint chain, add workload phases that view a past checkpoint and restore it forward, and add oracles for `state.restore` history and post-restore wake behavior. | `to_do` |
| 6 | **`historia-default` checkpoint revalidation** — Run the `examples/bundles/historia-default/scenarios` catalog under checkpoint durability pressure and include its selected substrate and bundle-local oracles in the Phase 10 proof artifacts. | `to_do` |
| 7 | **Release-gate matrix update** — Add any new nemesis profiles and scenario catalogs needed by tasks 2-6 to the local/CI suite contract, then prove `ivm` and `noivm` run the full matrix with the new-invariant oracles enabled. | `to_do` |
| 8 | **Phase verification** — Re-read this phase's directive and exit signal in the [README](../README.md). Confirm the full suite under every nemesis on both runtimes, including the new-invariant oracles, passes in CI as the release gate. If anything is missing, add rows above this one and rerun. | `to_do` |
