import { makeManifest } from "../_shared/scenario.mjs";

export default makeManifest("jump-forward-basic", "A participant triggers jump-forward and the bundle calls AI, flag search, and projection sync.");
