# Phase 0 — Scaffold completion

> Status: `complete` · Directive and exit signal: [README](../README.md)

## Tasks

Each row is a unit of work — big enough to be a task, not an entire major project, and not a single trivial change. Progress is a few sentences max; for anything that grows beyond that, log the detail in [`scratchpad.md`](scratchpad.md) and keep the cell short. The last row is always a phase-verification task — re-read the directive and exit signal, walk the docs the phase touches, and confirm nothing was missed. If something was, add rows above the verification row and rerun.

| # | Task | Progress |
|---|---|---|
| 1 | **Tigris-canonical `c.state`** — Replace the Redis-backed branch in [`runtime/parent-actor/src/parent.mts`](../../runtime/parent-actor/src/parent.mts) with CBOR-to-Tigris flush plus an in-process cache and flush window. See [`docs-next/contract/storage.md`](../../docs-next/contract/storage.md). | `complete` — Parent now CBOR-encodes state at `state/<gameId>.cbor`, hydrates through an in-process cache, schedules a configurable flush window, honors explicit `c.state.flush()`, and flushes before planned sleep/deadline release. Production uses the Tigris S3-compatible bucket; local dev falls back to the same object-key layout under `var/tigris-local` when no bucket env is present. |
| 2 | **Tigris-canonical `c.blob`** — Per-game namespace at `blob/<gameId>/<key>` in Tigris with per-key and per-namespace byte/key budgets enforced at the parent. | `complete` — SDK and IPC now expose keyed `put/get/delete/list`; parent stores objects under `blob/<gameId>/<key>`, enforces 100 MiB namespace and 1024-key budgets before PUT, and keeps `blob-bytes`/`blob-keys` budget snapshots current. Both child runners bridge bytes as base64 over JSON IPC while bundles see `Uint8Array`. |
| 3 | **Tigris bundle upload pipeline** — Replace the Redis-only bundle manifest in [`orchestration/control-plane/src/store.mts`](../../orchestration/control-plane/src/store.mts) with PUT-to-Tigris + sha256 verification, per [`docs-next/subsystems/bundle-storage.md`](../../docs-next/subsystems/bundle-storage.md). | `complete` — `POST /admin/bundles/:bundleName` now requires source, writes source/manifest/metadata to the object store under `bundles/<bundleName>/`, verifies source sha256 by reading it back, then finalizes the Redis metadata row without embedding source. |
| 4 | **Shard-side bundle cache** — Pull-on-cold-wake from Tigris with a local cache bounded by working set. | `complete` — Parent fetches object-backed bundles on cold wake, verifies `contentSha256`, stores cache hits under `PAX_BUNDLE_CACHE_DIR`, refreshes mtime on use, and prunes least-recently-used files to `PAX_BUNDLE_CACHE_MAX_BYTES`. |
| 5 | **Shard image Dockerfile** — Multi-stage build at [`runtime/shard-image/`](../../runtime/shard-image/) bundling Rivet engine + parent + both child runners + IPC schema. | `complete` — Added a multi-stage Dockerfile that builds vendored `rivet-engine`, installs/builds the Node workspace pieces the shard needs, bundles example bundle fallbacks, and assembles a runtime image with an entrypoint that starts the engine and parent actor together. |
| 6 | **Scenario-runner workload phase executor** — Implement the declarative phases (`open-sessions`, `send-json`, `expect-history-events`, `register-api-kinds`, `close-sessions`, `wait`) from [`docs-next/subsystems/scenario-runner.md`](../../docs-next/subsystems/scenario-runner.md). | `complete` — Runner now has a live phase executor for `seed-fixtures`, `register-api-kinds`, `open-sessions`, `send-json`, `wait`, `close-sessions`, and `expect-history-events`, using control-plane admin REST, placement router, and real WebSockets. Later stress/nemesis phase families remain separate tasks. |
| 7 | **Scenario-runner live oracle gate** — Run all seventeen guarantee oracles against the live run's history and emit `result.json` with pass/fail per oracle. | `complete` — Non-replay runs now execute the live workload, read the resulting history, run the same result/oracle pipeline as replay, and the oracle index covers guarantees 1–17. CLI exit remains non-zero for any non-pass oracle while still writing `result.json`. |
| 8 | **Nemesis runtime injector** — Turn the two existing nemesis config files into scheduled admin-REST actions driven by the runner. | `complete` — Live runs now start a `NemesisRuntime` alongside workload phases; `no-faults` is a no-op, and `shard-death-every-5m` schedules `kill-shard` injections that select eligible shards via `GET /admin/shards`, call `POST /admin/shards/:id/drain`, record driver history, and unblock `await-nemesis`. |
| 9 | **Host-event admin endpoint** — `POST /admin/games/:id/host-event` in the control plane with `wakeOnDelivery: true` handling. | `complete` — Control plane accepts host-event POSTs, emits `onHostEvent.received`, drops best-effort events for asleep games, and durably queues deliverable events in Redis with the 30-day host-event TTL. |
| 10 | **Host-event wake path** — Route a control-plane host-event for a sleeping game through the placement router. | `complete` — For sleeping `wakeOnDelivery` events, control plane calls the placement router and opens a short synthetic Rivet WebSocket to trigger actor start; the synthetic player is rejected by the parent allowed-player gate after wake, while the queued host event drains through the parent. |
| 11 | **`onHostEvent` IPC + parent + child** — Channel in [`shared/ipc-protocol`](../../shared/ipc-protocol/), parent dispatch, child handler invocation, `onHostEvent.delivered` history event. | `complete` — Added `OnHostEventPayload`/`HostEventRecord`, parent→child `onHostEvent`, SDK handler typing, both child-runner dispatchers, parent host-event queue draining, and `onHostEvent.delivered` history with event id, payload, and delivery attempt count. |
| 12 | **Guarantee #17 oracle** — `testing/oracles-lib/src/guarantees/host-event-durability.mts`. | `complete` — Added `host-event-durability.mts`, exported it in the guarantee index as G17, and extended history-completeness required fields for `onHostEvent.received`/`onHostEvent.delivered`. |
| 13 | **Drain ACK closure** — Parent reports drained, control plane emits `shard.drain.completed`, `GET /admin/shards/:id` reflects state. See [`docs-next/subsystems/redeploy-and-drain.md`](../../docs-next/subsystems/redeploy-and-drain.md). | `complete` — Shard registrations now carry `healthy`/`draining`/`drained`/`unhealthy`; parent reports `drained` once a draining shard has zero active games, and control plane emits `shard.drain.completed` once when admin shard reads or drain requests observe that state. |
| 14 | **Vector sidecar configs** — `scripts/observability/vector-prod.toml` and `vector-local-dev.toml` with the cardinality firewall transforms. | `complete` — Added prod and local Vector configs with Prometheus scrapes, OTLP ingress, log/history enrichment, Tigris history archive, local exporter/file sinks, and Prometheus-bound label culling for raw game/session/player/actor/trace/request/bundle/database IDs. |
| 15 | **OTel SDK wiring (Node)** — `@opentelemetry/sdk-node` in parent, control plane, gateway, reference URL services. | `complete` — Added a shared Node telemetry package that starts `NodeSDK` with OTLP/gRPC export to the local Vector endpoint, wired parent/control/gateway startup, and added explicit `gateway.invoke` plus `urlsvc.<kind>.invoke` spans for reference URL-service dispatch. |
| 16 | **OTel SDK wiring (Rust)** — `tracing-opentelemetry` in the placement router. | `complete` — Placement router now layers `tracing-opentelemetry` over the existing fmt subscriber, exports OTLP/gRPC traces to Vector by default, stamps resource attributes, and makes `router.placement` inherit inbound W3C `traceparent`. |
| 17 | **Engine OTel pass-through** — `RIVET_OTEL_ENABLED=1` on the vendored engine; confirm spans flow with parent context. | `complete` — Local and shard-image engine launchers now default `RIVET_OTEL_ENABLED=1`, set `RIVET_OTEL_GRPC_ENDPOINT` to the same Vector OTLP endpoint, and keep sampler ratio at 1.0 unless overridden; vendored source inspection confirms the flag initializes Rivet's OTLP provider and guard/API middleware propagates OTel context. |
| 18 | **SDK module split** — Break the single-file [`sdk/runtime-sdk/src/index.mts`](../../sdk/runtime-sdk/src/index.mts) into per-contract modules (`manifest.ts`, `lifecycle.ts`, `storage.ts`, `compute-budgets.ts`, `external-api-channel.ts`). | `complete` — Runtime SDK now has focused contract modules for manifest validation, lifecycle handlers, storage, compute budgets, external API calls, and the aggregate context while `src/index.mts` preserves the original root exports. |
| 19 | **`bundle publish` CLI** — Add to [`sdk/bundle-tools/`](../../sdk/bundle-tools/) a command that `POST`s to `/admin/bundles/:bundleName` against a running control plane. | `complete` — `pax-bundle publish <pkg> --control-plane-url <url> --bundle-name <name>` reads `dist/bundle.js`, extracts the manifest, and POSTs `{ manifest, source }` to the control plane; the built CLI lazy-loads commands so publish runs without verify-only dependencies. |
| 20 | **Reference URL service hardening** — Verify `echo.v1`, `delay.v1`, `http.fetch.v1`, `mock-ai.v1` all set `X-Gateway-Envelope-Version: 2` and that the `http.fetch.v1` allowlist is enforced. | `complete` — Gateway dispatch stamps `X-Gateway-Envelope-Version: 2` for all four reference kinds, co-located reference routes reject mismatched/missing envelope versions before handler dispatch, and `http.fetch.v1` rejects non-allowlisted plus non-HTTP(S) targets. |
| 21 | **Last-disconnect sleep grace** — Align parent lifecycle with [`docs-next/contract/lifecycle-and-wake.md`](../../docs-next/contract/lifecycle-and-wake.md): after the last session closes, start the 60s sleep-grace, send `onSleep` with `reason: "idle"` on expiry, flush before release, and make drain completion count sleeping games as released. | `complete` — Parent now starts/cancels an idle sleep-grace timer around last disconnect/reconnect, emits `onSleep` with `reason: "idle"` on expiry, flushes before releasing the active-game row, and reports drained once released games are asleep. IPC wake/sleep reason enums and the history catalog now match the docs. |
| 22 | **Phase verification** — Re-read this phase's directive and exit signal in the [README](../README.md). Walk every [`docs-next/`](../../docs-next/) page and code path the phase touches; confirm every subtask above has been enumerated and that the exit signal is actually met. If anything is missing, add rows above the verification row and rerun. | `complete` — Re-read the Phase 0 directive and scanned docs/code drift. One missing lifecycle item was added as task 21 and completed; after rerun, remaining scan hits are narrative/future-facing docs, not scaffold gaps. Root `pnpm typecheck` is deferred to Phase 1 because it is blocked by the existing billing-mock example type error. |
