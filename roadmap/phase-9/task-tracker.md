# Phase 9 — Fly topology proof

> Status: `complete` · Directive and exit signal: [README](../README.md)

## Tasks

Each row is a unit of work — big enough to be a task, not an entire major project, and not a single trivial change. Progress is a few sentences max; for anything that grows beyond that, log the detail in [`scratchpad.md`](scratchpad.md) and keep the cell short. The last row is always a phase-verification task — re-read the directive and exit signal, walk the docs the phase touches, and confirm nothing was missed. If something was, add rows above the verification row and rerun.

| # | Task | Progress |
|---|---|---|
| 1 | **Surface audit** — Re-read this phase's directive/exit signal and the deploy surfaces (`fly.*.toml`, `scripts/fly/*`, `scripts/bootstrap/*`, the observability pipeline, Fly-proxy WS routing). Expand this placeholder into a concrete task list. | `complete` — Audit split Phase 9 into bootstrap/deploy health, Fly-proxy WebSocket replay correctness, a 100-game topology scale plan, no-fault and shard-death evidence runs, observability trace proof, and closeout verification. Key finding: current WS pinning uses query params, while Fly's documented machine routing is headers or server-side `fly-replay`, so Broker upgrade handling needs a replay path before production proof. |
| 2 | **Bootstrap + deploy health** — Run `scripts/bootstrap/spin-up.sh`, deploy the control, shard, and driver images, confirm Fly checks, Redis/Tigris access, Better Stack secret sync, and no per-shard RocksDB volume in the shard app. | `complete` — Bootstrap converged idempotently, all three images were built and pushed, control and shard health checks are passing, and the shard app was rebuilt as exactly three no-volume Broker machines. Better Stack values are present in Infisical and on live machines without printing secrets; driver image is deployed and remains stopped until proof runs need it. |
| 3 | **Fly-proxy WebSocket routing** — Replace or validate the current query-param pinning with a documented Fly-proxy path: public shard hostname plus Broker-side `fly-replay: instance=<machine>` before WS upgrade when the request lands on the wrong shard. Prove the returned `webSocketUrl` is public and machine-pinned end to end. | `complete` — Deployed proof passed against the three Phase 9 shard machines. The router returned `wss://pax-backend-shards.fly.dev/gateway` with a placement token and no instance query params; a normal WS reached `ready`; a forced wrong-machine WS produced `connection.replay` on the non-target Broker and reached `ready` on the target machine. Evidence is in `var/phase-9/ws-routing-proof.json` with trace `b540aa83108b4a4589ea628f243f6b45`. |
| 4 | **Phase 9 topology scale plan** — Add or adapt a 100-game, multi-shard, 30-minute scale rung for the new Broker/Runner topology so the proof is not named as Phase 5 and can validate machine routing under at least two shard machines. | `complete` — Added `testing/scale-ladders/phase9-topology.mts` with rung `100g-3shards-30m-topology`: 100 games, three shard machines, 30-minute target, no-fault and shard-death nemeses, and bounded one-minute heartbeat fanout. The scenario-runner loader validates the plan. |
| 5 | **No-fault topology proof** — Run the 100-game Phase 9 topology rung on Fly under `no-faults`, collect artifacts/history/metrics, and fix any substrate issue until all guarantee oracles are green. | `complete` — Patched scale history waiting cleared the previous premature control-plane-local history failure. The fresh Fly run at `/data/phase-9/topology/ivm-20260529T122658Z` passed the `no-faults` case: 100 games placed across all three shards (34/33/33), `send-json` held for 1,770,276 ms, all 100 sessions closed normally with `1000/scenarioComplete`, and all scenario oracles passed with no workload failures or session errors. |
| 6 | **Shard-death topology proof** — Run the same 100-game topology rung under `shard-death-every-5m`, collect artifacts/history/metrics, and fix any substrate issue until all guarantee oracles are green. | `complete` — The same Fly run passed the `shard-death-every-5m` case and exited `0`: 100 games placed across all three shards (34/33/33), six drain/replacement cycles completed, `send-json` held for 1,770,280 ms, all 100 sessions closed normally with `1000/scenarioComplete`, and all scenario oracles passed with no workload failures or session errors. |
| 7 | **Trace exemplar** — Confirm the production Vector/Better Stack path contains at least one continuous placement-through-URL-service trace exemplar from the Phase 9 run, with trace ID and lookup notes recorded. | `complete` — Focused Fly run `phase9-trace-20260529T135059Z` passed all scenario oracles with trace `f40d0b0e10154668cf9e84793f3e8c25`: one placement/session, 40 API requests, 40 wire records, 40 responses, and normal session close. Better Stack table `t527589_pax_backend_v1_soak_2_logs` contained 229 rows for the trace, including the Broker history events plus OTLP span names `router.placement`, `gateway.invoke`, and `urlsvc.mock-ai.v1.invoke`; temporary query connections were deleted after lookup. |
| 8 | **Phase verification** — Re-read this phase's directive and exit signal in the [README](../README.md). Confirm ~100 games sustain 30 minutes on Fly with all oracles green under no-fault and shard-death, and one continuous trace exemplar is visible. If anything is missing, add rows above this one and rerun. | `complete` — Re-read the Phase 9 directive and exit signal. The topology proof run `ivm-20260529T122658Z` sustained 100 games for the 30-minute window under both `no-faults` and `shard-death-every-5m` with all scenario oracles green, and Better Stack contains the continuous trace exemplar `f40d0b0e10154668cf9e84793f3e8c25` from `router.placement` through `gateway.invoke` and `urlsvc.mock-ai.v1.invoke`. |
