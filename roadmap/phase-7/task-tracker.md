# Phase 7 — Runtime rewrite (code only)

> Status: `in_progress` · Directive and exit signal: [README](../README.md)

## Tasks

Each row is a unit of work — big enough to be a task, not an entire major project, and not a single trivial change. Progress is a few sentences max; for anything that grows beyond that, log the detail in [`scratchpad.md`](scratchpad.md) and keep the cell short. The last row is always a phase-verification task — re-read the directive and exit signal, walk the docs the phase touches, and confirm nothing was missed. If something was, add rows above the verification row and rerun.

| # | Task | Progress |
|---|---|---|
| 1 | **Surface audit** — Re-read this phase's directive/exit signal and the [`docs-next/`](../../docs-next/) pages and code surfaces it touches (`runtime/`, `shared/ipc-protocol`, `sdk/`, `orchestration/`, `testing/oracles-lib`, `examples/bundles/historia-default`, `fly.shards.toml`, `vendor/rivet`). Expand this placeholder into a concrete task list. | `complete` — Audit found the expected old-runtime residue: Rivet shard image/scripts/env, `parent-actor`, per-game child runners, Rivet WS protocols, `applySyncPromise`, Rivet-shaped shard rows, and scenario-runner/observability assumptions. |
| 2 | **Workspace/runtime package reshape** — Replace the old runtime package topology with production-shaped Broker, Runner, and state-store packages. Update workspace/package references while preserving the no-ivm conformance runtime. | `complete` — Added Broker, Runner, and state-store workspace packages with root project references. The packages are skeletal but production-shaped enough for later tasks to fill IPC, Broker logic, Runner internals, and checkpoint behavior without extending the old Rivet package topology. |
| 3 | **Async Broker/Runner IPC contract** — Rewrite `shared/ipc-protocol` around request-id async messages, Broker-stamped identity, assignment validation, Runner telemetry, and state/blob/ws/api/lifecycle channels. Remove any contract path that assumes synchronous isolate calls. | `complete` — `@pax-backend/ipc-protocol` now exposes primary Broker/Runner envelopes, channel catalogs, assignment grants, Runner/isolate telemetry, and typed async request/response channels. Narrow parent/child aliases remain only to keep legacy packages compiling until their Phase 7 implementation tasks replace them. |
| 4 | **Credential-holding Broker core** — Implement the per-shard Broker: WS termination/JWT verification, session lifecycle, allowed-player checks, idempotent input, budget enforcement, history writes, capacity watermarks, Runner-pool supervision, gateway/Redis/Tigris egress, and admin/health/metrics surfaces. | `complete` — Core Broker class owns JWT/allowed-player WS acceptance, sessions, Runner assignment validation, `c.*` request dispatch, state/blob/ws/api budget checks, history writes, capacity rows, planned release checkpoints, drain/evict commands, and health/ready/metrics/admin HTTP surfaces. Redis directory/allowed-player, gateway HTTP, JSONL history, bundle resolver, local source, and S3/Tigris source adapters are in place. |
| 5 | **One-state-object state store** — Implement the Broker-owned state cache and checkpoint engine: root object format, write-back cache, conditional root PUT fencing, blob manifest, checkpoint scheduler, GC hooks, and time-travel view/restore scaffolding. | `complete` — State-store now has a byte-cache session, inline/object state refs, immutable retained roots, conditional head PUT, blob manifest updates, checkpoint scheduler, superseded-object deletion, orphan reaper, checkpoint listing/view, revert-forward restore, and memory/local/S3 object-store adapters. |
| 6 | **Credential-less Runner pool** — Implement ivm and no-ivm Runners that host many games per process, expose a frozen async `c.*` shim, enforce handler CPU timeout and isolate memory caps, emit per-isolate telemetry, and hold no credentials/network/fs access. | `complete` — Added no-ivm, isolated-vm, and child-process RunnerProcess implementations. Runner workers host many games, expose only the frozen async `c.*` bridge, correlate assign/invoke completion over request-id IPC, forward telemetry/events to the Broker, and keep credentials/network/storage in the Broker. |
| 7 | **Router/control/gateway integration** — Update placement-router, control-plane, gateway, host-event wake, drain/redeploy, and Redis shard rows to the Broker/Runner model: no Rivet actor fields, no guard/tunnel routing, and Fly-proxy machine-pinned WS URLs. | `complete` — Placement router now has the Broker-row `POST /placement` path with active-game stickiness/claiming, Broker JWT claims, and direct Broker WS URLs with Fly machine hints. Control-plane host events queue through Redis and ask the target Broker admin endpoint to drain/deliver; the Broker can wake a game, drain queued host events, and invoke `onHostEvent`. Control-plane shard drain/un-drain calls Broker admin drain/resume, and the primary shard/active-game row schemas no longer expose Rivet actor fields. |
| 8 | **Dev/Fly packaging and observability** — Replace the Rivet shard image/local-up path with Broker+Runner startup, remove engine build/vendor steps from runtime images, update Fly env/TOML, and rename metrics/vector surfaces from parent/Rivet to Broker/Runner. | `complete` — Added a Broker runtime server that composes Redis/Tigris adapters, state store, child-process Runner pool, Broker admin, and WS upgrades. The shard image, Fly shard TOML, local-up/down scripts, and Vector profiles now use Broker/Runner startup and Broker metrics instead of `rivet-engine`, `parent-actor`, parent metrics, or Rivet metrics. |
| 9 | **Tests, scenarios, and historia compatibility** — Update smoke bot, scenario-runner live WS client, scale metrics, oracles, and `historia-default` expectations for the new transport/state model and new-invariant coverage. | `complete` — Smoke and scenario live clients use `POST /placement` plus direct Broker WebSockets, scale metrics scrape Broker surfaces, Broker history now stamps request/session/lifecycle events for the new model, and the guarantee/scenario oracles accept Broker/Runner event names while retaining legacy replay compatibility. |
| 10 | **Legacy runtime path removal** — Remove the old Rivet parent/per-game child packages, root workspace references, Rivet build helpers, stale shard volume scaling, and related docs/build context exceptions so the active runtime path contains only Broker/Runner code. | `complete` — Verification found the legacy parent/child packages were still part of the root workspace and typecheck graph. Removed those packages from the active tree, dropped Rivet package overrides/build scripts, moved Fly shard scaling/bootstrap off Rivet env and per-shard RocksDB volumes, and updated runtime/docs comments to Broker/Runner naming. |
| 11 | **Phase verification** — Re-read this phase's directive and exit signal in the [README](../README.md). Walk every `docs-next/` page and code path the phase touches; confirm each maps to production-shaped code, every package typechecks, and no `applySyncPromise` / Rivet runtime dependency / process-per-game model remains. If anything is missing, add rows above this one and rerun. | `complete` — Re-read the Phase 7 directive/exit signal, audited the docs-next-to-code mapping across Broker, Runner, state-store, placement, control/admin, gateway, observability, scenarios, and historia surfaces, and reran the forbidden-residue scan. `pnpm typecheck`, `cargo check --manifest-path orchestration/placement-router/Cargo.toml`, changed shell `bash -n`, and `git diff --check` are green. |
